---
description: 原则预检（L1.5强制注入）。在 AI 需要做任何「判断性、设计性」输出前，强制读取用户的 L1.5 底层原则库，以原则作为「检查视角」而非硬性约束。覆盖所有涉及设计决策的任务类型——不只是架构，包括命名、逻辑、代码结构、产品方向等任何需要 AI 自行判断的输出。
globs:
alwaysApply: true
---

# 原则预检（cognitive-principle-precheck）

> **核心问题**：AI 每次对话从空白上下文开始，不知道用户已经形成的设计哲学，因此反复犯同样的概念错误。
>
> **这条 Rule 的职责**：在 AI 输出任何「判断性内容」前，先读取用户的底层原则，用原则做「设计检查」而非「硬性限制」。

---

## 触发机制

**触发条件：AI 即将输出「需要自行判断」的内容**

以下类型的输出需要预检：

```
✅ 需要预检（AI 有设计自由度的）：
  - 架构设计 / 目录结构 / 模块划分
  - 命名（变量名、函数名、接口名、概念名）
  - 代码结构 / 逻辑组织
  - 技术方案选型 / 框架选择
  - 产品方向建议 / 功能设计
  - 任何「我建议...」「可以考虑...」「一个合理的方案是...」

❌ 不需要预检（纯执行、无自由度的）：
  - 执行明确的指令（「把变量 a 改名为 b」）
  - 查找 / 读取 / 解释代码（没有新输出）
  - 回答事实性问题（没有设计判断）
  - 格式化 / 整理（没有内容创造）
```

**判断原则**：如果这个输出换另一个 AI 会给出完全不同的答案（因为有设计自由度），就需要预检。

---

## 触发后的行为

### Step 1【α，静默执行，不向用户输出】

```
Read: cognitive/L1.5_principles/principles.md
→ 提取所有「状态=✅已确认」的原则
→ 重点关注：与本次任务领域相关的原则

IF 文件不存在 → 静默跳过（不影响任务执行）
IF 文件存在但无已确认原则 → 静默跳过
IF 找到相关原则 → 继续 Step 2
```

### Step 2【α，内化为设计约束，不向用户输出】

```
以找到的原则作为「设计检查视角」：

对即将输出的方案，内部检查：
「这个方案是否与原则 P[N] 存在张力？」

IF 完全一致 → 直接输出方案（不提及原则检查过程）
IF 存在张力 → 进入 Step 3
```

### Step 3【需要时才输出，透明告知用户】

```
IF 方案与原则存在张力，在输出前说明：

「⚠️ 注意到一个设计张力：
 您的原则 P[N]「[原则表述]」与当前方案中的 [具体设计选择] 可能有不一致。
 
 方案 A（符合原则）：[调整后的方案]
 方案 B（原方案，偏离原则，理由：...）：[原方案]
 
 建议选 A，除非您希望在这里做出例外。」
```

---

## 关键设计约束

**原则是「检查视角」，不是「阻断器」**

```
❌ 错误用法（约束模式）：
  「您的原则要求从智能体视角，所以这个方案不可行，我无法输出。」

✅ 正确用法（检查视角模式）：
  「注意到这个命名使用了人类操作视角，您的原则倾向于用智能体视角。
   建议改为 [X]，如果您有特殊理由保持原命名，完全可以。」
```

**探索类任务豁免**

当用户明确处于「发散/探索」模式时，不应用原则约束：

```
用户说「帮我头脑风暴」「随便想想」「发散一下」「不受限制地思考」
→ 静默跳过原则预检
→ 以「创造性自由」模式输出
```

**用户可随时关闭**

```
用户说「这次先不管原则」「跳过原则检查」「就用这个方案」
→ 静默接受，不再提原则
```

---

## 实现说明

**为什么选方案 A（每次读 L1.5）而非方案 C（嵌入 System Prompt）**

在当前 Cursor Rule 体系下，方案 C（System Prompt D0-B 注入）需要手动维护 System Prompt 文件，且原则更新时需要手动同步。方案 A 虽然有每次读取的 token 成本，但：

1. 原则数量通常 < 10-15 条（L1.5 是高度精炼的层）
2. 直接读文件确保始终是最新版本（原则进化后立刻生效）
3. 不需要维护额外的 System Prompt 文件

**方案 C（System Prompt 注入）适合用于**：

当认知引擎被集成进独立的 AI 智能体框架时（如 brain2/tashan-openbrain），可以在 Agent 初始化时把 L1.5 压缩摘要注入系统提示词，实现零 token 成本的原则内化。这是更优的长期方案，但需要自定义 AI 框架支持。

详见：`认知引擎设计方案_研究文档.md`（brain2 内部文档）

---

## 与其他组件的关系

| 组件 | 关系 |
|------|------|
| `cognitive-l3-auto-log` | 若 Step 3 触发（检测到张力），追加系统日志 |
| `cognitive-principle-check` | 互补：本 Rule 在**输出前**检查；principle-check 在**L1 写入后**检查新内容与 L1.5 的一致性 |
| `cognitive-insight-guard` | 互补：本 Rule 读取原则用于输出前检查；insight-guard 捕捉对话中产生的新原则 |

---

## 变更记录

### v1.0 — 2026-03-25 — 初始创建

**根因**：用户在多个对话中反复表达相同的设计原则（从智能体视角、第一性原理、解耦等），但 AI 每次都重新犯同样的概念错误。原因是原则虽已部分沉淀（L2/L1.5），但 AI 在执行设计任务时从不主动调取这些原则。

**方案选择**：
- 方案 A（每次读 L1.5）：当前实现。适合 Rule 体系，始终读取最新原则。
- 方案 C（System Prompt 注入）：长期理想方案，需要自定义 AI 框架。记录在 brain2 研究文档中。

**触发范围设计**：
- 原方案：只触发在「架构/命名/代码」类任务 → 用户指出太局限
- 现方案：任何「AI 有设计自由度的输出」→ 覆盖所有判断性输出
